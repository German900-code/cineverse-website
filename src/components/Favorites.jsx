import Header from "./Header";
import MobileNav from "./MobileNav";

const Favorites = () => {
  return (
    <section>
      <Header />
      <MobileNav />
    </section>
  );
};

export default Favorites;
